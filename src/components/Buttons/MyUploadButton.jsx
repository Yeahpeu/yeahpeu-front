const MyUploadButton = ({ value }) => {
  const handleUpload = () => {
    console.log("upload");
  };
  return (
    <form action="">
      <button type="submit">업로드</button>
    </form>
  );
};

export default MyUploadButton;
