const MyInputWhite = ({ type, placeholder }) => {
  return (
    <input
      type={type}
      placeholder={placeholder}
      className="border border-gray-300 rounded-lg p-2 w-full"
    ></input>
  );
};

export default MyInputWhite;
